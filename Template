`OWNER` 
> [insert here] 

```BIO```
`GENDER`
> [insert here]
`SEXUALITY`
> [insert here]
`AGE`
> [insert here] cycles *(as of Act -8)*
`BIRTHDAY`
> [insert here (calculate by counting in-world month + date the character was made irl)]
`LATIN NAME MEANING`
> [latin name] = [meaning]
`ALLEGIANCES DESCRIPTION`
> [insert here]
`DETAILED DESCRIPTION`
> [insert here] 

```LINKS```
`Toyhouse:` <TBA>
`Wiki:` <TBA>
`Spotify:` <TBA>
`Pinterest Board:` <TBA>
`Server bio:` TBA

```FACTION```
`FACTION`
> [insert here]
`RANK`
> [insert here]
`FORMER NAME(S)`
> [insert here]
`MENTOR`
> [insert here if applicable]
`APPRENTICE(S)`
> [insert here if applicable]
`MATE(S)`
> [insert here] 

```BLOODLINE```
`BIOLOGICAL PARENT(S)`
> [insert here]
`ADOPTIVE PARENT(S)`
> [insert here]
`BIOLOGICAL SIBLING(S)`
> [insert here]
`HALF SIBLING(S)`
> [insert here]
`ADOPTIVE FARTLING(S)`
> [insert here]
`BIOLOGICAL CHILD(REN)`
[insert here]
`ADOPTIVE CHILD(REN)`
> [insert here] 

```TRIVIA```
`VC`
> [insert here]
`THEME`
"TITLE" - ARTIST
> :cd: <LINK>
`INSTRUMENTAL`
"TITLE" - ARTIST
> :cd: <LINK>
`OTHER`
> ?

/// (only include the Obituary if your character is deceased)

```OBITUARY```
 `CAUSE OF DEATH`
> [insert here (include killer(s) if applicable)]
 `TIME OF DEATH`
> [insert here (either which event or pre-arc I (list how many cycles ago if possible!))]
`AGE AT DEATH`
> [insert here] cycles 

/// (only include "old refs" or "+ mini head" if there is content for these.)
```OLD REFS```
[insert image(s) here]

```CURRENT REF + MINI HEAD```
[insert images here]
